# HANDOFF — 2026-07-10 (resume-from-disk)

Written at a context reset. **Start here.** `TASKS.md` = the backlog (Batches 5 & 6 are current),
`MAINTENANCE.md` = app internals, `git log` = the blow-by-blow. Supersedes `HANDOFF-2026-07-09.md`.

---

## 1. The big picture — this is now the "MISFITS DATABASE"

The app is no longer just the Tribe. It's a **multi-faction umbrella** ("Misfits Database") hosting
factions/sections. **8 factions** exist (`FACTIONS` in app.js), each a config *skin* (name, brand,
theme colour+bg, its own data sheet, its own doc tabs). Switching faction re-skins the whole app +
loads that faction's data. The **Tribe** (rust/amber, default) and **Brotherhood of Steel** (red)
have REAL data wired; the other 6 (Vault/Legion/Bazaar/Enclave/Unity/NCR) are unlinked → show a
themed "coming soon" roster until a contributor adds a sheet. Adding a faction = one config entry.

The database keeps reading **Google Sheets**. Docs are read from **Google Docs** (Tribe Lore /
Roleplay Guide). The docs are being migrated INTO a **wiki** — see T35.

---

## 2. What shipped this session (2026-07-10) — all pushed to `dr-orac/oracular-db` main

### Batch 10 (2026-07-11) — roster tidy + CodePen image-tint crib
- **Roster section headers unstuck** + **filter bar aligned to the search** (left inset --sp-2, right at
  the divider) with the divider rule removed (18205e8).
- **Images tinted to the theme with one CSS filter** (fa68514) — `sepia()`+`hue-rotate()` (hue computed
  per theme via `hslOf`→`--img-hue`/`--img-sat`) replaces the washed grey+multiply wash on portraits +
  doc images (multiply kept only for inverted line-art). Effectively resolves T37 for portraits; doc
  images use the identical filter (verify live). Crib from carterfromsl's OJjxXKJ (CREDITS.md).
- ⏳ **"Recently edited" sort — BLOCKED (no data).** The gviz CSV has NO per-row edit timestamps, so
  true "last edited" isn't derivable. Reverse-sheet-order is an unreliable proxy (rows get inserted
  anywhere). PROPER FIX: add an "Updated"/"Last edited" column to the sheet (auto-stamped by a tiny
  onEdit Apps Script the user adds — never write to CONFIG.sheetId ourselves), then sort by it. Also
  note: the list is section-GROUPED, so a global recency order would need a flat/ungrouped list mode.
  Awaiting the user's call on the column + grouping.

### Batch 9 (2026-07-11) — masthead controls + home-tile glow-up
- **Masthead rebuilt** (090897f) — the wordmark-as-dropdown is replaced by a **FACTION box**: a
  bordered panel with a "FACTION" legend on the top border, the current faction name (display face),
  and a big solid Fallout chevron. Whole box = trigger; menu now animates in (slide+fade+scale, options
  staggered via `--i`), arrow rotates. Settings is a **large cog glyph on the far right** (`.cogbtn`,
  turns on hover/open). **"// PIP-LINK" tagline dropped** (masthead + document.title); dead wordmark
  rules (workbench/chassis/mobile) retired. Chassis brand signage removed — box carries identity in
  both modes now (chassis-specific box styling is a T12 follow-up).
- **Home tiles → nav-cards** (7412c41) — left accent bar · framed icon · left-aligned title+blurb ·
  faded 01/02/03 index watermark · "Enter →" CTA on hover; hover lifts + glows + theme-wash + arrow
  slide; staggered entrance. Responsive + reduced-motion-safe.

### Batch 8 (eve) — exterior bezel, text-size stepper, reading progress
- **Text-size − / + stepper** (13c63a0) — replaced the jumpy Cozy/Comfortable/Large/X-Large tiers with
  a numeric px stepper (`--root-fs`, 1px steps, hard 17px floor; − disables at the floor). A committed
  size is ABSOLUTE (font change no longer swings it); "Auto" restores the font-aware default (pixel
  faces start larger). Model: `effectiveTextPx()`/`stepTextSize()`/`renderTextSize()`/`autoSizeForFont()`.
- **Reading-progress ring** (58bfb1d) — the doc back-to-top button is now a circular marker whose arc
  fills with reading progress (`--docpct` from `#docscroll`); centre arrow still jumps to top. One control.
- **Exterior screen bezel reworked** (8a27c59) — `--bezel-black/gap/line/in/radius` vars drive a
  concentric-ring `body:not([data-frame="border"])::after`: jet-black band ~1mm thicker (16px) →
  screen-bg gap → rounded **theme-coloured hairline** (tracks `--fg`) + faint glow. `--edge` min
  raised so content always clears the hairline; the title row got a real top inset (`--bezel-in +
  --sp-5`) that clears the top bezel; the header divider runs full-bleed out to the hairline. Scales
  down on mobile; still toggles fully off (Screen Bezel = off). SCREEN MODE ONLY (Chassis untouched).
- ⏳ **CodePen crib (T47) — PENDING SOURCE.** codepen.io/carterfromsl/pen/OJjxXKJ 403s to fetch and no
  export zip is on disk; user chose "skip for now". Revisit when they paste the code / drop the zip.

### Batch 7 (pm) — header relayout, declutter, architecture, bug
- **Faction selector → top-right corner** (924ac0b) — the brand wordmark itself is now the trigger,
  with a large boxed ▼ (rotates when open); menu opens right-aligned. Header collapsed to ONE row
  (tabs left · Settings + selector right). Dropped the "SWITCH FACTION" pill.
- **Search dropped below the divider line** (924ac0b) — command bar moved OUT of `<header>` into a
  `.commandbar` (child of `.app`) that mirrors the header padding, so the search lines up exactly with
  the list column beneath it. Section-filter + sort are one chunky **abutting** segmented bar.
- **All dropdowns in-theme** (eba35c3) — native `<select>`s replaced by a reusable `.selctl` custom
  dropdown (themed trigger + phosphor popup, one delegated handler); faction + section + sort now match.
- **Architecture consolidation** (6368491) — `fkey(aspect)` + `factionAppearance(id)` collapse the
  repeated per-faction key-building and "override-else-signature" resolution (was duplicated in
  applyFaction / init / refreshCur). No behaviour change; one place to touch per faction.
- **BUG FIX: extras scoped by their own faction** (6a96a00) — EXTRA_CHARACTERS now carry a `faction`
  field (default "tribe") and merge on the CHARACTER's affiliation, not the viewer's. Lily Weaver
  (Brotherhood Head Knight) tagged `brotherhood` — she left the Tribe roster (55) and now shows in
  Brotherhood (12). ⚠ NOTE: the NCR/merc "Sage's Goobers" extras (Azalea/Shannon/Stacey) still default
  to tribe — user only flagged Brotherhood; revisit if they should move to NCR.

### Batch 6
- **HOME landing page** (commit d355f47) — `#home` section + `renderHome()` build full-height section
  tiles (icon+title+explainer); Home tab + glyph; Home is the default landing view; universal (survives
  faction switch, re-renders per faction); command bar hidden on Home. CSS `.home*`.
- **Faction dropdown made unmistakable** (de0a62c) — "SWITCH FACTION ▾" pill beside the wordmark
  (`.brand-switchbox`), hover-brighten, caret flips ▲ when open, re-tints per faction, gentle ~5s pulse
  (`@keyframes brandpulse`, off under prefers-reduced-motion).
- **Per-faction signature FONTS** (1bc6e13) — each `FACTIONS[id].font={head,body}` loads on switch/init
  like signature colours; overrides remembered per faction (`yuma-font-head-<faction>`/`-body-<faction>`,
  legacy global keys migrated once then retired). Tribe=Fallouty, Brotherhood=Monofonto/Share Tech,
  Vault=Overseer(the real Vault-Tec face)/Plex, Legion=Pixelify, Bazaar=DotGothic16, Enclave=Fixedsys,
  Unity=Fallouty(green), NCR=VT323. Bodies all legible (Plex/Share Tech). Also **renamed the FACES
  `fallout` label "Fallout 1·2" → its real name "Fallouty"** (Sébastien Caisse 2002; key stays `fallout`).
  Doc-reader fonts (--doc-font-*) still on their own defaults — a possible follow-up is to key those per
  faction too.
- **Statusline removed** (ONLINE/UNITS/SYNC) — tightened the top; command bar now aligns with no hack.
- **Command bar + index rail no longer jump** between roster/doc views (docbar moved into the header).
- **Nav always shows** (≥ the Roster tab) so it holds place across factions.
- **8-faction system**: `FACTIONS`/`FACTION_ORDER`, masthead **dropdown switcher**, per-faction
  colour (new THEMES: orange/purple/rust; NCR=cyan), coming-soon state, per-faction `document.title`,
  per-faction docs. **Faction data correctly scoped**: `EXTRA_CHARACTERS`, default section, and the
  `relabelTribe` rebrand are all **tribe-only** now (they were leaking into every faction).
- **Brotherhood roster wired** (sheet `1hG6V1ddnlr8jZZm8Rg0jJ4360WrH7zUD-TvNqaPkuUg`, gid 735572717):
  column aliases (`Full name`→name, `Caste and Rank`→role) + Brotherhood-specific FIELDS
  (Origin/Preferred Armament/Talents/Medical History/Accomplishments/Status) added to FIELDS + the
  ID/BLOCK render orders. Tribe unchanged (56 chars + extras).
- **Doc reader (Batch 5)**: TOC now h1–h4 + ≥17px + clean hierarchy (**T23**); quote-bullets styled
  consistently (block-level `.dq` detection — **T24**); multi-cell tables restyled (**T25**);
  **line-art diagrams auto-invert + colourise** (`.docfig-invert`, mostly-white detection — **T26**,
  ⚠ NEEDS A LIVE VISUAL CHECK, couldn't screenshot in headless); **1×1-table "line box" quotes →
  framed pull-quotes** (`.docquote-box`) shipped earlier.
- **Batch 6 quick fixes**: settings drawer uses ONE fixed legible font (IBM Plex Mono, decoupled from
  the app font); removed colour/bg swatch circle previews; **removed the Screen Density control**;
  removed the portrait corner-bracket reticle; brightened tinted images.
- **Doc images**: now fade on **all four edges** + sit inside **~10% side buffers** (not full-bleed;
  full-bleed on mobile). Inverted diagrams keep `mask:none` (don't fade). Brightness/tint tuned to
  a moderate value — ⚠ **still reads a bit washed on illustrations; wants a final by-eye tune.**

---

## 3. IMMEDIATE next tasks (the user asked, not yet done)

1. ~~Text-size +/- stepper (T36)~~ — DONE (13c63a0).
2. **Image brightness/tint final tune (T37)** — the 4-edge fade + buffers are in; the tint
   (`.docfig-tint` opacity .42) + `.docfig img` brightness(1.16) still leave illustrations a touch
   washed. Tune by eye on the live site (an image *with text*, like a screenshot, is the real test).
   ⚠ Headless preview can't reliably hydrate the large lazy base64 doc images — needs a real browser.
3. **T26 live check** — confirm the flow chart inverts nicely on the live site; tune the light-ratio
   threshold (`classifyDocImage`, 0.55) if a photo/illustration wrongly inverts.
4. **T32 leftover (full-width auto-dividing nav tabs)** — RECONSIDER: the header is now ONE row (tabs
   left, faction selector top-right), so "stretch tabs full-width" conflicts with the selector sharing
   the row. The tabs are already prominent (icons + chunky). Revisit only if the user still wants it.

---

## 4. Roadmap — the design-led Batch 6 pieces (do before the wiki)

- **T31 · Screen border** — jet-black bezel (like a real Apple display) + a **rounded hairline** where
  the rounded bezel meets the app; must work in chassis AND screen-only modes.
- **T30 · Image fade-in on scroll** + a lazy-load audit (portraits/cards).
- **T32 · Navigation + home-page redesign — LARGE, DESIGN-FIRST, needs the user's eye.**
  - ✅ **HOME landing page SHIPPED** (commit d355f47): `#home` section + `renderHome()` build a tall,
    full-width tile per section (icon + title + one-line explainer, from `HOME_INFO`/`NAV_ICONS`);
    a Home tab + home glyph joined the nav; Home is the default landing view (`setSection("home")` at
    init). Home + Roster are universal (survive a faction switch, tiles re-render for the new docs).
    Command bar + toolbar hidden on Home. CSS: `.home`/`.home-tiles`/`.home-tile*`.
  - ✅ **Faction dropdown made unmistakable** (commit de0a62c): a labelled "SWITCH FACTION ▾" pill
    beside the wordmark (bordered control, hover-brighten, caret flips to ▲ when open, re-tints to the
    active faction). Carries the **gentle occasional top-bar pulse** the user asked about (~5s cycle,
    off under prefers-reduced-motion). Markup in `renderBrand`; CSS `.brand-switchbox`/`@keyframes
    brandpulse`.
  - ⏳ STILL TO DO on T32: top section buttons **much larger — stretch to full width of their row,
    auto-dividing by count** (the nav tabs are still fixed-width chips).
- ~~**T33 · Rename → "Misfits Database"**~~ — DONE (b1bfc9a): index.html title/description/OG +
  og:site_name, boot line, per-character stub site_name (43 regen'd), OG card image redrawn, README +
  MAINTENANCE. Per-faction `document.title` stays per faction. ⚠ NOT done: renaming the GitHub repo /
  deployed URL (would break `dr-orac.github.io/oracular-db`) and the `Yuma Tribe Roster` source folder
  (kept for history) — do those deliberately if ever wanted.
- ~~**T34 · Reading-progress marker**~~ — DONE (58bfb1d): the doc back-to-top button is a circular
  ring filling with `--docpct`.

## 5. Then — T35 · Wiki reading (AUTHORISED)

The Google Docs are being migrated INTO the Misfits **wiki**; target = view the WIKI well.
**Keep the Google-Doc reader working; the database keeps reading Sheets; wiki is an ADDITIONAL source.**
Generalise the content source (`{type: gdoc|wiki, source}`), fetch + sanitise wiki HTML through
`docClean`, handle wiki nav/internal links. (Relaxes the old Substrate+Lattice gate. Lattice = a
Substrate-linked UI builder with standard HTML layouts, in the Claude project folder.)

---

## 6. Pointers
- Repo `dr-orac/oracular-db` (GitHub Pages). Read-only Sheets — NEVER write to `CONFIG.sheetId`.
- Verify: `python3 tools/preview.py` → browser-preview MCP → `python3 tools/selfcheck.py`.
- Conventions: imperative commit subject + why-body, **no AI-signal trailers**, no AI-gen trademarks
  on the public site. One task = one commit.
- **Preview gotcha:** `tools/preview.py` injects `window.YUMA_SHEET_OVERRIDE` (the tribe mirror), which
  wins over the faction sheet in `effectiveSheetId()`. To test a non-tribe faction's REAL data in
  preview, `delete window.YUMA_SHEET_OVERRIDE` then `applyFaction('brotherhood')`. Production has no
  override, so faction sheets load normally.
- **Doc images are large lazy base64** — hard to hydrate under programmatic scroll in headless; scroll
  gently or call `_figSet(img, _docHydrateCtx.images)`. They render fine in a real browser.
